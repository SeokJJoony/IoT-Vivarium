#include<stdio.h>
#define MAX_COUNT 7

void main()
{
	int data[MAX_COUNT] = {9,4,11,7,2,3,6};
	int i,MAX=0;
	int MaxIndex=0;
	for(i = 0; i<7; i++)
	{
		if(MAX<data[i])
		{
			MAX=data[i];
			MaxIndex = i+1;
		}
	}
	printf("최댓값은 : %d이고 순번은 %d번째입니다.",MAX,MaxIndex);
}
